import { AppManifest } from "../common"

export const manifest: AppManifest = {
  name: "M-Pesa Import",
  description: "Turn an M-Pesa statement CSV export into categorized income and expense transactions",
  icon: "📱",
}
